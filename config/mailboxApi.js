var Mailbox = require('./models/mailbox'); // container to hold all messages allows us to have an index
var MailboxMessages = require('.models/mailboxmessages');

module.exports = function(router, jwt, app) {

    //allows us to modulate our api calls


};
